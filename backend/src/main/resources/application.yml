server:
  port: 8080

spring:
  application:
    name: netflix-clone-backend

firebase:
  config-path: serviceAccountKey.json

tmdb:
  api-key: ${TMDB_API_KEY:4e44d9029b1270a757cddc766a1bcb63}
  base-url: https://api.themoviedb.org/3

logging:
  level:
    root: INFO
    com.netflix.clone: DEBUG
